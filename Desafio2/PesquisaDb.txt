SELECT Numero, P.Nome, DataVancimento, DataProrrogacao, Valor + (Valor*Acrescimo) + (Valor*Desconto) AS Valor_Líquido
FROM Pessoas AS P
INNER JOIN ContasPagas AS Cp ON P.Codigo == Cp.CodigoFornecedor
INNER JOIN ContasAPagar AS Cap ON P.Codigo == Cap.CodigoFornecedor
